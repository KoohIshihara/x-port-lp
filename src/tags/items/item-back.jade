item-back
  a.warp-item.f#back
    img(src='./img/icon-back.svg')
    p.ml12 戻る

  style(scoped, type='less').
    :scope {
      display: block;
      :hover {
        opacity: 0.2;
        transition: 0.2s;
        cursor: pointer
      }
      .warp-item {
        p {
          font-size: 14px;
          color: #fff;
          font-weight: bold;
        }
      }
    }

  script.
    this.on('updated', function(){
      $('#back').on('click',function(){

        /*
        setTimeout(function(){
          debugger
          TO_SCROLL.toScroll('module-works');
        }, 400);
        */
        location.href = './#works';

      });
    });
